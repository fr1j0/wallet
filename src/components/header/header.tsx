import React from "react";

const Header = () => {
  return (
    <header className="fixed w-full h-16 border-b-2 bg-white flex items-center px-4">
      <div>Wallet</div>
    </header>
  );
};

export default Header;
